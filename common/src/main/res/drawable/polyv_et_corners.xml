<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android" >

    <item>
        <shape>
            <solid android:color="@color/bottom_et_color_gray" />

            <corners android:radius="@dimen/corners_common" />
        </shape>
    </item>

    <!--除了item中的边距的背景色，内圆角,除了item中的边距的边距大小的背景色(透明会被覆盖)  -->
    <item android:right="0.8dp" android:bottom="0.8dp" android:left="0.8dp" android:top="0.8dp">
        <shape >
            <solid android:color="@color/top_edittext_color_gray"/>
            <corners android:radius="@dimen/corners_common"/>
        </shape>
    </item>

</layer-list>